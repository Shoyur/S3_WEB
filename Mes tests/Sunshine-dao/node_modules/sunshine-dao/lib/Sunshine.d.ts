import { Db } from "mongodb";
/**
 *  Sunshine DAO Connector
 *
 *  @ Michael Hasler
 */
export declare class Sunshine {
    protected static db: Db;
    protected static properties: any;
    protected static isConnected: boolean;
    private static mongoClient;
    private static eventEmitter;
    static setEncryptionKey(key: string): void;
    static getEncryptionKey(): any;
    static connectURI(uri: string, encryptionKey?: string): Promise<unknown>;
    static connect(hostname: string, username: string, password: string, database: string, encryptionKey?: string): Promise<unknown>;
    static injectConnection(db: Db): void;
    static on(event: string, callback: (event: any) => void): void;
    static event(name: string, payload: any): void;
    static getConnection(): Db;
    static disconnect(): Promise<boolean>;
}
